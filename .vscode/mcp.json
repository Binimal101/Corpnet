// Producer & Consumer are now FastAPI HTTP servers.
// Start them with:
//   uvicorn producer.api:app --host 0.0.0.0 --port 8000
//   uvicorn consumer.mcp_server:app --host 0.0.0.0 --port 8001
{
  "servers": {}
}